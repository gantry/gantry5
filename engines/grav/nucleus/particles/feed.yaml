name: Feed
description: Feed Particle for Grav Feed Plugin
type: particle
icon: fa-rss

dependencies:
  platform:
    grav:
      plugin:
        feed: true

form:
  fields:
    enabled:
      type: checkbox
      label: Enabled
      description: Globally enable feed particles.
      default: true
