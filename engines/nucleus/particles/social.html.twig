{% extends '@nucleus/partials/particle.html.twig' %}

{% block particle %}
    <div class="g-social">
        {% for item in particle.items %}
            <a target="{{ particle.target }}" href="{{ item.link }}">
                <span class="{{ item.icon }}"></span>
            </a>
        {% endfor %}
    </div>
{% endblock %}