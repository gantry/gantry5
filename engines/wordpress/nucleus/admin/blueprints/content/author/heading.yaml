name: Author Page Heading
description: Options for displaying Heading
type: author

form:
  fields:

    heading:
     type: input.checkbox
     label: Enabled
     description: Display custom heading text at the top of the page.
     default: 1

    heading.text:
      type: input.text
      label: Custom Heading
      description: Text to be used as a custom heading.
