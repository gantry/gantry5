name: Query
description: Options for the query in the blog page
type: blog

form:
  fields:

    categories.include:
      type: wordpress.categories
      label: Include Categories
      description:

    categories.exclude:
      type: wordpress.categories
      label: Exclude Categories
      description:

    posts.leading:
      type: input.text
      label: Leading Posts
      description:
      default: 1

    posts.intro:
      type: input.text
      label: Intro Posts
      description:
      default: 3

    posts.columns:
      type: input.text
      label: Columns
      description:
      default: 1
