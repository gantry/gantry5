{% set html %}
    {% if segment.children %}
        {% for segment in segments %}
            {% include '@nucleus/layout/' ~ segment.type ~ '.html.twig' with { 'segments':segment.children } %}
        {% endfor %}
    {% endif %}
{% endset %}

{% if html|trim %}
    <div class="g-grid{{ segment.classes ? ' ' ~ segment.classes|join(' ')|e }}">
      {{ html|raw }}
    </div>
{% endif %}


