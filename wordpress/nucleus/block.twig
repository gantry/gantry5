<div class="block{{ segment.classes ? ' ' ~ segment.classes|join(' ') }}">
    {% if segment.children %}
        {% for segment in segments %}
            {% include '@nucleus/' ~ segment.type ~ '.twig' with { 'segments':segment.children } %}
        {% endfor %}
    {% endif %}
</div>
