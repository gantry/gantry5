<?php

/**
 * @name GANTRY_VERSION
 */
define('GANTRY5_VERSION', '${project.version}');

if (!defined('DS')) {
    define('DS', DIRECTORY_SEPARATOR);
}

if (!defined('DEBUG')) {
    define('DEBUG', true);
}
