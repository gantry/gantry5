{% extends "partials/page.html.twig" %}
{% set twigTemplate = 'buddypress.html.twig' %}
{% set scope = 'buddypress' %}

{% block content %}

    {# Enable WordPress specific CSS styling #}
    {% do gantry.theme.wordpress(true) %}

    <div class="platform-content">
        <div class="buddypress-content">

            <article class="g-content post-type-{{ post.post_type }} {{ post.class }}" id="content-{{ post.ID }}">
                {{ content }}
            </article>

        </div>
    </div>

{% endblock %}
