{% extends 'partials/particle.html.twig' %}

{% block particle %}
<div class="g-block g-popupmodule-button {{ particle.css.classname }} g-particle">
    {% if not me.ID %}
        <a data-rokbox="" data-rokbox-element="#g-popuplogin" href="#" class="buttontext button">
            <span class="desc">{{ particle.logintext }}</span>
        </a>
    {% else %}
        <a href="{{ function('wp_logout_url') }}" class="buttontext button">
            <span class="desc">{{ particle.logouttext }} {{ my.name|e }}</span>
        </a>
    {% endif %}
</div>
{% endblock %}
