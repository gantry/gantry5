<nav class="main-nav" role="navigation">
    <ul class="g-toplevel">
        {% for page in pages.children %}
            {% if page.visible %}
            <li>
                <a href="{{ page.url }}">{{ page.menu }}</a>
            </li>
            {% endif %}
        {% endfor %}
    </ul>
</nav>
