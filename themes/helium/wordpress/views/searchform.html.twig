{% set twigTemplate = 'searchform.html.twig' %}
{% set scope = 'searchform' %}

{% block searchform %}

    <form role="search" method="get" class="search-form form" action="{{ site.url }}/">
        <label>
            <span class="screen-reader-text">{{ __('Search for:', textdomain) }}</span>
            <input type="search" class="search-field" placeholder="{{ __( 'Search …', textdomain ) }}" value="" name="s" title="{{ __('Search for:', textdomain) }}" />
        </label>
        <input type="submit" class="search-submit button button-small" value="Search" />
    </form>

{% endblock %}
