name: Menu
description: Gantry menu.
type: particle

form:
  fields:

    enabled:
      type: input.checkbox
      label: Enabled
      default: true

    title:
      type: input.text
      label: Menu Title
      default: mainmenu

    base:
      type: input.text
      label: Base Path
      default: /
