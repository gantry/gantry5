<!--<ul class="g-grid menu-selector">
    <li class="g-block">
        Home<i class="fa fa-cog"></i>
        <span class="fa fa-chevron-down"></span>
    </li>
    <li class="g-block active">
        Features<i class="fa fa-cog"></i>
        <span class="fa fa-chevron-down"></span>
    </li>
    <li class="g-block">
        Pages<i class="fa fa-cog"></i>
        <span class="fa fa-chevron-down"></span>
    </li>
    <li class="g-block">
        Styles<i class="fa fa-cog"></i>
        <span class="fa fa-chevron-down"></span>
    </li>
    <li class="g-block">
        Download<i class="fa fa-cog"></i>
        <span class="fa fa-chevron-down"></span>
    </li>
    <li class="g-block">
        Contact<i class="fa fa-cog"></i>
        <span class="fa fa-chevron-down"></span>
    </li>
</ul>-->

<section
    {%- if particle.css.id %} id="{{ particle.css.id|e }}"{% endif %}
    {%- if particle.css.class %} class="{{ particle.css.class|e }}"{% endif %} role="navigation">
    <ul class="g-grid g-toplevel menu-selector">
        {% for child in item %}
            <li>
                {% include 'menu/item.html.twig' with {'item': child, 'target': 'columns' } %}
            </li>
        {% endfor %}
    </ul>
</section>
<div class="column-container" data-g5-menu-columns="">
    {% if columns %}
        {% include 'menu/columns.html.twig' with {'item': columns } %}
    {% endif %}
</div>
